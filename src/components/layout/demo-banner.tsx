/** Shown while the app runs on example data. */
export function DemoBanner() {
  return (
    <p className="bg-brand-700 px-4 py-1.5 text-center text-xs font-medium text-white">
      Versión de prueba · los datos son de ejemplo
    </p>
  );
}
